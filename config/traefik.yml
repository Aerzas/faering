http:
  middlewares:
    https_redirect:
      redirectScheme:
        scheme: https
        permanent: true
tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/faering.crt
        keyFile: /certs/faering.key
